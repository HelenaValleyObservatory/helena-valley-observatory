# Almanac

Council session records. Each file is one session of the Observer Council.

Sessions are generated by `bin/council.py` and saved automatically.

The almanac is a permanent record. The observatory remembers.
